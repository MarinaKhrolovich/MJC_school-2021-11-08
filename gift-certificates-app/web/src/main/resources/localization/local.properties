message.resource.NotFound = Requested resource not found
message.resource.AlreadyExists = Resource already exists
message.somethingWrong = Something wrong, try again!

message.certificate.name.fill = The field 'name' of certificate should be filled!
message.certificate.name.length = Invalid length of name of certificate! It shouldn't be < 3 and > 100
message.certificate.description.fill = The field 'description' should be filled!
message.certificate.description.length = Invalid length of description!It shouldn't be < 3 and > 1000
message.certificate.duration.fill = The field 'duration' should be filled!
message.certificate.price.fill = The field 'price' should be filled! 
message.certificate.duration.value = Invalid duration! It should be > 0
message.certificate.price.value = Invalid price! It should be > 0

message.tag.name.fill = The field 'name' of tag should be filled!
message.tag.name.length = Invalid length of name of tag! It shouldn't be < 3 and > 45

message.path.id.min = Value of path variable 'id' should be > 0

message.user.login.fill = The field 'login' should be filled! 
message.user.login.length = Invalid length of login of user! It shouldn't be < 3 and > 45
message.user.name.length = Invalid length of name of user! It shouldn't be < 3 and > 45
message.user.surname.length = Invalid length of surname of user! It shouldn't be < 3 and > 45

message.order.user.id.fill = The field 'userId' should be filled! 
message.order.user.id.value = Invalid userId! It should be > 1
message.order.certificate.id.fill = The field 'certificateId' should be filled! 
message.order.certificate.id.value = Invalid certificateId! It should be > 1

