message.resource.notFound = Requested resource not found
message.resource.alreadyExists = Resource already exists
message.somethingWrong = Something wrong, try again!
message.resource.hasLinks = Resource couldn't be deleted! It has links!
message.resource.noLinks = One or more resources are has no links!

message.certificate.name.fill = The field 'name' of the certificate should be filled!
message.certificate.name.length = Invalid length of the certificate name! It shouldn't be < 3 and > 100
message.certificate.description.fill = The field 'description' should be filled!
message.certificate.description.length = Invalid length of the description!It shouldn't be < 3 and > 1000
message.certificate.duration.fill = The field 'duration' should be filled!
message.certificate.price.fill = The field 'price' should be filled! 
message.certificate.duration.value = Invalid duration! It should be > 0
message.certificate.price.value = Invalid price! It should be > 0

message.tag.name.fill = The field 'name' of tag should be filled!
message.tag.name.length = Invalid length of the tag name! It shouldn't be < 3 and > 45

message.path.id.min = Value of the path variable 'id' should be > 0
message.resource.id.min = Value of the variable 'id' should be > 0

message.user.login.fill = The field 'login' should be filled! 
message.user.login.length = Invalid length of the user login! It shouldn't be < 3 and > 45
message.user.name.length = Invalid length of the user name! It shouldn't be < 3 and > 45
message.user.surname.length = Invalid length of the user surname! It shouldn't be < 3 and > 45

message.order.user.id.fill = The user id should be filled! 
message.order.user.id.value = Invalid user id! It should be > 1
message.order.certificate.id.fill = The certificate id should be filled! 
message.order.certificate.id.value = Invalid certificate id! It should be > 1

